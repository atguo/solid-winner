/**
 * Created by luoop on 16-7-7.
 */
import * as Action from '../action/itemDetail'

let initState = {
}

const itemDetail = (state = initState, action) => {
    switch (action.type) {
        case Action.LOAD_DATA: {
        }
    }
}